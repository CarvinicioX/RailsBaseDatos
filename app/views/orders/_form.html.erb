<%= form_for(order) do |f| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :constancy_id , class: 'col-sm-2 control-label'%>
    <div class="col-sm-10">
      <%= f.number_field :constancy_id, class: 'form-control' %>
    </div>
  </div>

  <div class="field">
    <%= f.label :bill_id, class: 'col-sm-2 control-label' %>
    <div class="col-sm-10">
     <%= f.number_field :bill_id %>
    </div>
  </div>

  <div class="field">
    <%= f.label :type, class: 'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= f.text_field :type %>
    </div>
  </div>

  <div class="field">
    <%= f.label :observation, class: 'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= f.text_field :observation %>
    </div>
  </div>

  <div class="actions">
    <%= f.submit 'Guardar', class: 'btn btn-default btn-sm' %>
  </div>
<% end %>
