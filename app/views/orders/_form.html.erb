<%= form_for(order) do |f| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :constancy_id %>
    <%= f.number_field :constancy_id %>
  </div>

  <div class="field">
    <%= f.label :bill_id %>
    <%= f.number_field :bill_id %>
  </div>

  <div class="field">
    <%= f.label :type %>
    <%= f.text_field :type %>
  </div>

  <div class="field">
    <%= f.label :observation %>
    <%= f.text_field :observation %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
