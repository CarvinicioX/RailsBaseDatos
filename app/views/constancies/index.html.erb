<p id="notice"><%= notice %></p>

<h1>Constancies</h1>

<table>
  <thead>
    <tr>
      <th>Pacient</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @constancies.each do |constancy| %>
      <tr>
        <td><%= constancy.pacient_id %></td>
        <td><%= link_to 'Mostrar', constancy, class: 'btn btn-default btn-sm'  %></td>
        <td><%= link_to 'Modificar', edit_constancy_path(constancy), class: 'btn btn-default btn-sm'  %></td>
        <td><%= link_to 'Eliminar', constancy, method: :delete, data: { confirm: 'Está Seguro?' }, class: 'btn btn-default btn-sm'  %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nueva Constancia', new_constancy_path, class: 'btn btn-default btn-sm'  %>
