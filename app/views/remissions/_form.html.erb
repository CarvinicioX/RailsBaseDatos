<%= form_for(remission) do |f| %>
  <% if remission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(remission.errors.count, "error") %> prohibited this remission from being saved:</h2>

      <ul>
      <% remission.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :constancy_id %>
    <%= f.number_field :constancy_id %>
  </div>

  <div class="field">
    <%= f.label :laboratory_id %>
    <%= f.number_field :laboratory_id %>
  </div>

  <div class="field">
    <%= f.label :date %>
    <%= f.date_select :date %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
