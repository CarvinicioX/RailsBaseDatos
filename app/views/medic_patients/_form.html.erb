<%= form_for(medic_patient) do |f| %>
  <% if medic_patient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(medic_patient.errors.count, "error") %> prohibited this medic_patient from being saved:</h2>

      <ul>
      <% medic_patient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :patient_id %>
    <%= f.text_field :patient_id %>
  </div>

  <div class="field">
    <%= f.label :medic_id %>
    <%= f.text_field :medic_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
